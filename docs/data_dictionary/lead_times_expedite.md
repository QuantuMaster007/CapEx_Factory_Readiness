# lead_times_expedite.csv

**Row grain:** tool_id + item

Key columns:
- `lead_time_days_planned` vs `lead_time_days_actual`
- `expedite_spend_usd` and `expedite_reason`
- `need_by_date` / `promise_date` / `received_date` for delivery risk
